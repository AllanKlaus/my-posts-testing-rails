<%= form_for(@post, :html => {:class => 'ls-form'}) do |f| %>
  <% if @post.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@post.errors.count, "error") %> prohibited this post from being saved:</h2>

      <ul>
      <% @post.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="ls-label">
    <%= f.label :category_id %>
    <%= f.collection_select(:category_id, Category.all, :id, :category) %>
  </div>
  <div class="ls-label">
    <%= f.label :author %>
    <%= f.text_field :author %>
  </div>
  <div class="ls-label">
    <%= f.label :title %>
    <%= f.text_field :title %>
  </div>
  <div class="ls-label">
    <%= f.label :text %>
    <%= f.text_area :text %>
  </div>
  <div class="actions">
    <%= f.submit 'Submit', :class => 'ls-btn' %>
  </div>
<% end %>
